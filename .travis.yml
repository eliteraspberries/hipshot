language: python
python:
  - "2.7"
sudo: false
addons:
  apt:
    packages:
      - python-opencv
install:
  - pip install -r requirements.txt
script:
  - python setup.py build
virtualenv:
  system_site_packages: true
